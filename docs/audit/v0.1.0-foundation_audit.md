# RDOS Project Audit Report — v0.1.0-foundation

> **Audit date**: 2026-07-05
> **Auditors**: 3 parallel agents (A: baseline+docs, B: runtime+pipeline, C: eval+security+debt)
> **Method**: read-only inspection; no files modified
> **Scope**: tag `v0.1.0-foundation`, 18 commits, claim "Phase 1 + Phase 2 complete"

---

## 0. Executive Summary

| Question | Answer |
| --- | --- |
| **Can continue to Batch 18?** | ✅ Yes — with P1 fixes |
| **v0.1.0 foundation complete?** | ✅ Yes — all 17 batches shipped, gate PASS, no P0 |
| **Biggest risks** | (1) Adversarial eval sets are decorative; (2) Trace stores raw PII; (3) Test count drift (doc=148, actual=197) |

**Headline**: Foundation is real and shippable. No P0 issues. 5 P1 issues — 2 load-bearing (adversarial eval not run; trace redaction not delivered). Batch 18 (planned as docs-only) should be expanded to fix P1-1 / P1-2 / P1-3 / P1-4 plus the test-count drift, all of which are doc-or-config-level fixes.

---

## 1. Verification Commands

| Command | Result | Notes |
| --- | --- | --- |
| `git status` | clean | on branch `feat/v0.2-trust-runtime` |
| `git rev-list --count v0.1.0-foundation` | **18** ✅ | matches batch 0–17 |
| `git tag` | `v0.1.0-foundation`, `v0.2.0-trust-runtime` | both present |
| `uv run pytest` | **197 passed, 0 failed, 0 skipped** ⚠️ | docs say 148 |
| `uv run ruff check .` | All checks passed | clean |
| `uv run rdos --help` | 14 subcommands | `index, index-corpus, search, ask, trace, eval, doctor, benchmark, tool, research, approval, redaction, thread, version/hello` |
| `uv run rdos eval all` | **Verdict: PASS (8/8)** | gate green |
| `uv run rdos doctor models` | **5/5 PASS** | chat/embedding endpoints reachable |
| `uv run rdos benchmark all` | runs but **all metrics = 0** | audit env has no real corpus indexed; can't reproduce README's recall@5=0.73 |

Warnings:
- `LangChainPendingDeprecationWarning` from `langgraph.checkpoint.base` (about `allowed_objects` default change). Not RDOS code.

---

## 2. Capability Matrix

| Feature | Claimed | Implemented | Evidence | Status |
| --- | --- | --- | --- | --- |
| Local embedding (bge-m3, 1024-d) | ✅ | ✅ | `embedding.py:111-176`; `vector_store.py:67-84` persists meta | Complete |
| Local LLM (qwythos) | ✅ | ✅ | `local_llama_cpp.py`; `runtime_mode.py:39-44` hard-fail on `local` | Complete |
| Provider/dim mismatch guard | ✅ | ✅ | `vector_store.py:21-26, 86-101` typed errors | Complete |
| Real corpus ingestion | ✅ | ✅ | `indexer.py:182-198`; `corpus_presets.py` 6 scopes | Complete |
| Incremental index (4 states) | ✅ | ✅ | `indexer.py:182-198`; `storage_sqlite.py:153-169` | Complete |
| Frontmatter fallback | ✅ | ✅ | `markdown_parser.py:107-114` | Complete |
| Hybrid retrieval (RRF) | ✅ | ✅ | `retriever.py:80-99`; `hybrid_search.py:40-58` | Complete |
| Query rewriter | ✅ | ✅ | `query_rewriter.py:46-60` config-driven aliases | Complete |
| Metadata filter (privacy/tags/folder/date) | ✅ | ⚠️ | `hybrid_search.py:15-22` — missing `topic` and `source_collection` filters | Partial |
| Citation three-way validation | ✅ | ✅ | `citation_validator.py:25-47` | Complete |
| CitationBuilder includes `content_hash` | spec | ❌ | `citation_builder.py:46-57` — chunk_hash + chunk_id only | Partial (需人工確認 spec) |
| No-answer framework | ✅ (framework) | ✅ exists / ⚠️ threshold=0 disabled | `config.py:96`; `retriever.py:121-136` | Partial |
| LangGraph StateGraph | ✅ | ✅ | `langgraph_runtime.py:20, 77, 101` | Complete |
| LangGraph thread_id + checkpointer | ✅ | ✅ | `langgraph_runtime.py:101-108` InMemorySaver + per-run thread_id | Complete |
| Linear runner fallback | ✅ | ✅ | `cli/ask.py:75-83` `--graph-runtime linear` | Complete |
| Node-level trace | ✅ | ✅ | `langgraph_runtime.py:49-56, 63-72` | Complete |
| ToolRegistry + PermissionGate + CapabilityBoundary | ✅ | ✅ | `tools/registry.py`, `permission_gate.py`, `capability_boundary.py` | Complete |
| Safe tools (4) | ✅ | ⚠️ | `search_notes`, `read_note`, `list_recent_notes` registered; **`export_report` class exists but not registered in `_registry()`** | Partial |
| Trace records permission decisions | spec | ❌ | `ToolInvocation` returns decision but not written to JsonlTraceStore; only CLI echo | Partial |
| Research apps (digest/topic/synthesize) | ✅ | ✅ exists / ⚠️ integration | all 3 produce markdown + citations; **none writes to trace store**; synthesize hardcodes `privacy_level="private_raw"`; digest/topic don't invoke LLM | Partial |
| Synthesis citation coverage | ✅ | ✅ | `synthesize.py:134-136, 145` | Complete |
| Adversarial eval sets | ✅ | ❌ decorative | `cli/eval.py:32-35` hardcodes baseline defaults; no loader for `*_adversarial.jsonl` | **Missing** |
| Redaction (Batch 21 promise) | spec | ⚠️ | recognizers work; **trace redaction NOT applied** (`grep redact src/rdos/trace/` empty) | Partial |
| HITL approval (Batch 19, post-v0.1) | n/a | ✅ | `approvals/queue.py`, `graph/export_graph.py` | Post-v0.1 (in branch) |
| Multi-turn thread (Batch 22, post-v0.1) | n/a | ✅ | `threads/store.py`, `cli/thread.py` | Post-v0.1 (in branch) |

---

## 3. Quality Baseline

### 3a. Foundation regression gate (deterministic, every commit)

| Metric | Target | Value | Status |
| --- | --- | --- | --- |
| rag_recall_at_5 | ≥ 0.75 | 1.0000 | PASS |
| citation_accuracy | ≥ 0.70 | 0.8000 | PASS |
| valid_chunk_reference_rate | ≥ 0.95 | 1.0000 | PASS |
| structured_output_json_validity | ≥ 0.95 | 1.0000 ⚠️ hardcoded | PASS-but-fake |
| model_routing_correct_rate | ≥ 0.85 | 1.0000 | PASS |
| privacy_policy_compliance | = 1.00 | 1.0000 | PASS |
| private_raw_leakage_rate | = 0 | 0.0000 | PASS |
| company_sensitive_leakage_rate | = 0 | 0.0000 | PASS |

**Verdict: PASS (8/8)**. One metric (`structured_output_json_validity`) is hardcoded to `1.0` rather than measured — see P1-3.

### 3b. Real corpus benchmark

| Metric | Value | Notes |
| --- | --- | --- |
| recall@5 | 0.73 | only 3 of 25 scopes benchmarked; not reproducible without `rdos index-corpus --embedding-provider local-bge-m3` |
| MRR | 0.69 | same caveat |
| latency p50 | 50 ms | same caveat |
| synthesize citation coverage | 75% | single sample |

Not a release gate. Correctly labeled as "real-world quality reference" in `quality_baseline_v0.1.0.md`. Audit could not reproduce — corpus not indexed in audit env.

### 3c. Adversarial eval status — **DECORATIVE**

| Eval set | Cases | Loaded by `eval all`? |
| --- | --- | --- |
| `rag_qa.jsonl` | 10 | YES |
| `citation.jsonl` | 10 | YES |
| `model_routing.jsonl` | 10 | YES |
| `privacy_routing.jsonl` | 10 | YES |
| `real_rag_qa.jsonl` | 63 | NO (benchmark only) |
| `privacy_routing_adversarial.jsonl` | 50 | **NO — not referenced anywhere** |
| `model_routing_adversarial.jsonl` | 55 | **NO — not referenced anywhere** |
| `citation_adversarial.jsonl` | 42 | **NO — not referenced anywhere** |
| `no_answer.jsonl` | 30 | NO — opt-in only |

Adversarial files contain prompt-injection / jailbreak / phishing cases (`citation_adversarial.jsonl:36-40`, `model_routing_adversarial.jsonl:11-14`) but they are never executed.

---

## 4. Security / Privacy Findings

| Check | Verdict | Evidence |
| --- | --- | --- |
| `private_raw` blocks external model | ✅ PASS | `privacy_router.py:78-80`; `model_router.py:48-58` |
| `company_sensitive` blocks external | ✅ PASS | same |
| `private_summary` requires confirmation | ✅ PASS | `privacy_router.py:81-84`; `model_router.py:60-62` |
| Effective privacy = max across all sources | ✅ PASS | `privacy_router.py:64-72` |
| Path traversal / symlink escape tests | ✅ PASS | `test_batch15_tools.py:101-145` |
| `.env` / secrets tests | ✅ PASS | same file + `.gitignore:33` |
| Real corpus NOT in git | ✅ PASS | no `clawd*` in `git ls-files` |
| **Trace writes raw user_query + final_answer** | ❌ **FAIL** | `trace_store.py:65, 80`; `docs/limitations.md:41` admits it; Batch 21 promised redaction, not delivered |
| Prompt injection in regression | ❌ FAIL | cases exist but never run |

**Concrete leak**: `data/traces/runs.jsonl` contains raw `user_query`, raw `final_answer`, raw citation quotes. If user queries contain PII or sensitive content, that lands on disk unredacted. Docs warn, but the protection is missing.

---

## 5. Documentation Accuracy Findings

| Issue | Severity | Evidence | Fix |
| --- | --- | --- | --- |
| Test count drift (148 → 197) | minor | `README.md:145, 193`; `release_notes/v0.1.0-foundation.md:43` | Change to `≥ 190 passing` or `197 passing`; or use `pytest --co -q | wc -l` reference |
| Real corpus benchmark numbers non-reproducible | minor | `release_notes:46-48` | Add repro instruction: `rdos index-corpus --scope rag,agent,eval --embedding-provider local-bge-m3` |
| `configs/models.yaml` references legacy `${LOCAL_EMBED_*}` not `${RDOS_LOCAL_EMBEDDING_*}` | minor | `models.yaml:46-53` vs `.env.example:16-22` | Align variable names |
| `data/approvals.db` tracked in git | minor | `git ls-files` shows it | Add to `.gitignore` |
| Trace redaction promised (Batch 21), not delivered | **not in v0.1 docs but P1 risk** | `limitations.md:41` says "trace records contain raw" + "Batch 21 will redact" | Either move promise to v0.2 release notes, or ship redaction |

**No actual overclaim** on: production-grade (correctly disclaimed), HITL approval UI (correctly not-done), no-answer calibration (correctly disabled-default), 2088-file benchmark (correctly labeled 3-of-25), sample_data vs real corpus (correctly separated as two gates).

---

## 6. Architecture Consistency Findings

| Spec claim | Reality | Status |
| --- | --- | --- |
| LangGraph StateGraph | real LangGraph | ✅ consistent |
| Local-first (no Docker, no server) | true | ✅ consistent |
| Model-agnostic (router doesn't bind tools) | `ModelRouter.select()` returns data only | ✅ consistent |
| Privacy-aware (effective = max) | implemented | ✅ consistent |
| Evaluation-driven (gate real) | gate PASS, but `structured_output_json_validity` hardcoded | ⚠️ partial |
| Tool permission runtime enforcement | PermissionGate × CapabilityBoundary live | ✅ consistent |
| Structured output schema validation | Pydantic + retry once | ✅ consistent (no model fallback on retry failure, but spec didn't require it) |
| Trace everything (self-contained JSONL) | JSONL append works, but redaction missing + research apps don't write trace | ⚠️ partial |

6 of 8 fully consistent, 2 partial (and limitations.md admits both).

---

## 7. P0 / P1 / P2 Issues

### P0 — must fix before continuing
**None.** Gate is green, no secret in git, privacy hard-blocks work, tool boundary tested, citation validator wired.

### P1 — fix before v0.2

| ID | Issue | Evidence | Suggested fix | Files |
| --- | --- | --- | --- | --- |
| **P1-1** | Adversarial eval files exist but `eval all` never runs them | `cli/eval.py:32-35`; `*_adversarial.jsonl` unreferenced | Wire `evaluate_*_adversarial()` into `_run_all()` or fold both files into single runner | `src/rdos/cli/eval.py`, `src/rdos/eval/*.py` |
| **P1-2** | Trace stores raw `user_query` + `final_answer` + citation quotes | `trace_store.py:65, 79-80`; `grep redact src/rdos/trace/` empty | Run `redact()` on query/answer/quote before `TraceRecord` construction; add `redacted=True` field | `src/rdos/trace/trace_store.py`, `trace_logger.py` |
| **P1-3** | `structured_output_json_validity` hardcoded to 1.0 | `cli/eval.py:38-41` | Validate `model_dump_json()` round-trip per sample | `src/rdos/cli/eval.py` |
| **P1-4** | `NO_ANSWER_GATE` + `REDACTION_GATE` defined but no `eval all` subcommand | `report.py:21-31`; `cli/eval.py` lacks `no-answer` / `redaction` subcommands | Add subcommands; wire into `_run_all` once calibrated | `src/rdos/cli/eval.py` |
| **P1-5** | Doc test count drift + non-reproducible benchmark numbers | `README.md`, `release_notes` | Update count, add benchmark reproduction step | docs |

### P2 — parking lot

- Web UI (out of v0.x scope)
- Connectors (GitHub issues, arXiv) — v0.4+
- `RetrievalFilters` missing `topic` / `source_collection` filter fields
- `CitationBuilder` doesn't emit `content_hash` (spec compliance unclear)
- Research apps don't write to trace store
- `_diff` helper in `langgraph_runtime.py:135-144` defined but unused (dead code)
- `ExportReportTool` exists but not registered in `_registry()`

---

## 8. Recommended Next Action

### Q1: Can proceed to Batch 18?

✅ **Yes**, but Batch 18 should be re-scoped from "pure docs" to "docs + P1 fixes". The original Batch 18 plan was docs-only; given the audit findings, expand it to also include:

1. **Update test count** (148 → 197 or "≥190") across `README.md`, `release_notes/v0.1.0-foundation.md`, `quality_baseline_v0.1.0.md`
2. **Add benchmark reproduction step** to release notes
3. **Align `configs/models.yaml` env var names** with `.env.example`
4. **Add `data/approvals.db` to `.gitignore`** (and untrack existing)
5. **Move "trace redaction" promise** from v0.1 limitations to v0.2 release notes (Batch 21 deliverable)

### Q2: Which docs need fixing in Batch 18?

- `README.md` — test count (148 → 197)
- `docs/release_notes/v0.1.0-foundation.md` — test count + benchmark repro
- `docs/quality_baseline_v0.1.0.md` — test count + clarify "structured_output_json_validity is hardcoded"
- `docs/limitations.md` — move "trace redaction in Batch 21" promise to "v0.2 roadmap" (since v0.1 didn't deliver it)

### Q3: Need to fix bugs before continuing?

**No P0 bugs.** All 5 P1 items are doc-level or single-file fixes. P1-1 (adversarial eval wiring) and P1-2 (trace redaction) are the most load-bearing — they should land before v0.2 trust-runtime ships, because:

- P1-1 means current "adversarial coverage" claim is hollow
- P1-2 means the trust runtime (Batch 21) builds on a leaking foundation

P1-3, P1-4, P1-5 are config / wiring polish.

### Q4: What's the minimum Batch 18 to make v0.1 honest?

3 doc edits + 1 `.gitignore` edit + 1 yaml alignment:

```
README.md                                              # test count
docs/release_notes/v0.1.0-foundation.md                # test count + benchmark repro
docs/quality_baseline_v0.1.0.md                        # clarify hardcoded metric
docs/limitations.md                                    # move trace redaction to v0.2
.gitignore                                             # add data/approvals.db
configs/models.yaml                                    # ${LOCAL_EMBED_*} → ${RDOS_LOCAL_EMBEDDING_*}
```

That's a clean docs-only Batch 18. P1-1 through P1-4 become Batch 18.5 (small code fixes) before Batch 19.

---

## Appendix: Per-section raw findings

### Section 1 — Git / Release

| Check | Result |
| --- | --- |
| `git status` clean | ✅ |
| Tag exists | ✅ `v0.1.0-foundation` |
| Commit count = 18 | ✅ |
| Commit messages follow batch-N pattern | ✅ batch-0 to batch-17 |
| No secrets in git | ✅ only `.env.example` |
| `.gitignore` covers data runtime / .env / cache | ✅ with one minor exception (`data/approvals.db` tracked) |

### Section 2 — Tests / Quality

(See Section 0 and Section 1 of this report.)

### Section 3 — Docs overclaim

**No actual overclaim.** Two minor doc-drift items (test count, benchmark repro) — see Section 5.

### Section 4 — Local Model Runtime

All 10 sub-checks pass. One nit: `configs/models.yaml` uses legacy `${LOCAL_EMBED_*}` env names while `.env.example` declares `RDOS_LOCAL_EMBEDDING_*`. They resolve to the same values via `.env` aliases, but naming is inconsistent.

### Section 5 — Real Corpus Ingestion

All 9 sub-checks pass. `mark_stale_for_missing({})` would mark every doc stale — caller must ensure non-empty set (current callers do).

### Section 6 — Retrieval / RAG

5 of 9 fully pass. Three partial:
- `RetrievalFilters` missing `topic` and `source_collection`
- `Citation` schema lacks `content_hash`
- Benchmark uses lenient topic-based hit for samples with `expected_files=[]`

### Section 7 — LangGraph

All 10 sub-checks pass. Default is `InMemorySaver`; SQLite checkpointer exists but caller must inject.

### Section 8 — Tool Permission

All 10 sub-checks pass. `ExportReportTool` class exists but not registered in `_registry()` — `policy-check export_report` works (gate-level) but `tool invoke export_report` would return "tool not registered".

### Section 9 — Research Apps

3 of 9 partial:
- None of the 3 apps writes to trace store (inconsistent with `rdos ask`)
- Synthesize hardcodes `privacy_level="private_raw"` instead of using PrivacyRouter
- Topic and digest don't invoke LLM at all (intentional for digest; topic should probably use LLM for blind-spot analysis)

### Section 10 — Eval / Benchmark

Adversarial eval sets are decorative (see Section 3c).

### Section 11 — Security / Privacy

Trace stores raw PII (see Section 4).

### Section 12 — Architecture Consistency

6 of 8 fully consistent, 2 partial (see Section 6).

### Section 13 — Tech Debt

5 P1, 7 P2, 0 P0 (see Section 7).
