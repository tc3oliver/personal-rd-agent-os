# RDOS v1.0.1 - Real-runtime Validation Fix

This is a maintenance-only patch release.

## Fixed

- Fixed `export_graph.py` writing the report path instead of synthesis markdown content during HITL approval resume.

## Validation

Post-release validation confirms:

1. Real HITL approval lifecycle:
   synthesize -> approval_required -> approve -> resume -> markdown report written.

2. Real multi-turn research thread:
   3 turns with local-bge-m3 + qwythos and cited context carry-forward.

## Scope

- No new features
- No architecture changes
- No new apps
- No new connectors
- Maintenance-only patch

## Status

RDOS remains feature-complete after v1.0.1.
Future work is intentionally parked.
